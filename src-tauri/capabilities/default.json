{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the DeepSeek Harness control window. The shell plugins are deliberately absent: the desktop frontend only talks to the local control commands.",
  "windows": ["main", "harness"],
  "permissions": ["core:default"]
}
